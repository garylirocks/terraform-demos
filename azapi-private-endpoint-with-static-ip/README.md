# Use AzApi provider to create a private endpoint resource with static ip

The `azurerm_private_endpoint` could not create a private endpoint with a static private IP.

We use the `AzApi` provider here for it