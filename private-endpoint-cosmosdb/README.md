# Private endpoint to a CosmosDB

Configuring multiple static IPs for a CosmosDB private endpoint.

One for the global endpoint `cosmosdb-xxx`, another for the regional endpoint  `cosmosdb-xxx-{region}`.